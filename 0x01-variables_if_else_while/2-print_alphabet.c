#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry Point
 *
 * Return: 0 Always(Success)
 */
int main(void)
{
	char c;
	for (c == "A";, c <="Z";, ++c;)
	{
		putchar("%c", c);
	}
	return (0);
}
